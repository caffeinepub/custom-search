{
  "frontend/src/App.tsx": "Root application component that renders the SearchInterface as the sole view inside a full-height flex container.",
  "frontend/src/components/SearchInterface.tsx": "Complete search interface with a centered form, loading/error/empty states, flattened DuckDuckGo RelatedTopics rendering in cards, and in-app browser integration for opening result links.",
  "frontend/src/hooks/useSearch.ts": "Custom hook managing search state — calls the backend actor's `search` method with a URL-encoded query, validates JSON responses, extracts readable ICP canister trap error messages, and exposes `{ searchResults, isLoading, isActorFetching, error, performSearch }`.",
  "frontend/src/index.css": "Global styles defining an OKLCH-based warm coral/teal color system for both light and dark modes, with Tailwind directives and a fade-in animation utility.",
  "frontend/tailwind.config.js": "Tailwind configuration mapping OKLCH CSS variables to semantic color tokens, with custom border-radius, shadow, font-family, and accordion keyframe extensions.",
  "frontend/src/utils/urlFormatter.tsx": "Utility that detects URLs in text and converts them to clickable elements; accepts an optional `onLinkClick` callback to open links in the in-app browser instead of navigating natively.",
  "frontend/src/components/InAppBrowser.tsx": "Fullscreen modal overlay that renders an iframe for external URLs, with a header bar, loading spinner, and automatic blocked-content detection (via load-timing heuristic and onError). When a site blocks iframe embedding (X-Frame-Options / CSP), a friendly fallback panel is shown with a prominent \"Open in new tab\" button and a \"Go back to results\" option."
}